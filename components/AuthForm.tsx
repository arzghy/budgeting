"use client";
import { signIn } from "next-auth/react";
import { FormEvent } from "react";
export default function AuthForm({mode}:{mode:'login'|'register'|'reset'}){async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(mode==='login'){const d=new FormData(e.currentTarget);await signIn('credentials',{identifier:d.get('identifier'),password:d.get('password'),callbackUrl:'/dashboard'})}}if(mode==='reset')return <form onSubmit={submit} className="space-y-5"><label className="block text-sm font-semibold">Email<input type="email" required className="mt-2 w-full rounded-sanctuary border border-sanctuary-ink/15 bg-sanctuary-rose/45 px-4 py-3"/></label><button className="w-full rounded-full bg-sanctuary-ink px-6 py-4 font-bold uppercase text-sanctuary-lime">Kirim tautan</button></form>;return <div className="space-y-5"><button type="button" onClick={()=>signIn('google',{callbackUrl:'/dashboard'})} className="flex w-full items-center justify-center gap-3 rounded-full border border-sanctuary-ink/15 bg-white px-6 py-4 font-bold uppercase"><img src="/images/icons8-google.svg" alt="" className="size-5"/>Masuk dengan Google</button><form onSubmit={submit} className="space-y-5"><label className="block text-sm font-semibold">{mode==='login'?'Email atau username':'Nama'}<input required className="mt-2 w-full rounded-sanctuary border border-sanctuary-ink/15 bg-sanctuary-rose/45 px-4 py-3"/></label><label className="block text-sm font-semibold">{mode==='login'?'Kata sandi':'Email'}<input type={mode==='login'?'password':'email'} required className="mt-2 w-full rounded-sanctuary border border-sanctuary-ink/15 bg-sanctuary-rose/45 px-4 py-3"/></label><button className="w-full rounded-full bg-sanctuary-ink px-6 py-4 font-bold uppercase text-sanctuary-lime">{mode==='login'?'Masuk':'Daftar'}</button></form></div>}
